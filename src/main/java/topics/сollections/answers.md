# 1. Что описывает и показывает Big O нотация как высчитывается, что фактически измеряется, привести примеры.

### 📌 Что такое Big O?
**Big O нотация** — это способ оценки сложности алгоритмов, который описывает зависимость скорости роста времени 
выполнения алгоритма от количества элементов, который этот алгоритм обрабатывает.

#### Что фактически измеряет Big O?

Big O не измеряет точное время выполнения алгоритма. Вместо этого оно показывает зависимость количества операций от 
размера входных данных (n).

 Что измеряет Big O?

1. Временная сложность – как изменяется количество операций при увеличении n. 
2. Пространственная сложность – сколько дополнительной памяти использует алгоритм. 

---

### Виды сложности

| Нотация    | Название                   | Пример |
|------------|----------------------------|--------|
| `O(1)`     | **Константная**            | Доступ к элементу массива `arr[i]` |
| `O(log n)` | **Логарифмическая**        | Бинарный поиск |
| `O(n)`     | **Линейная**               | Простой проход по массиву |
| `O(n log n)` | **Линейно-логарифмическая** | Быстрая сортировка (quicksort) |
| `O(n²)`    | **Квадратичная**           | Сортировка пузырьком |
| `O(2ⁿ)`    | **Экспоненциальная**       | Решение задачи коммивояжера |
| `O(n!)`    | **Факториальная**          | Перебор всех перестановок |

###  График сложности:

![Big O график](https://cdn.javarush.com/images/article/83910563-3deb-4f82-932c-fa3f28157d78/512.webp)

---
### Как вычисляется Big O?

Big O анализ проводится в несколько шагов:
1. Определяем количество операций в зависимости от размера входных данных n.
2. Отбрасываем константы – O(2n) → O(n), O(5n²) → O(n²).
3. Оставляем доминирующий член – O(n² + n) → O(n²).
4. Учитываем вложенные циклы – O(n × n) = O(n²).
5. Определяем худший случай.

___

## Примеры

#### Временная `O(n)`, пространственная `O(1)`

```java
public int findMax(int[] arr) {
    int max = arr[0]; // O(1) - используем только 1 переменную
    for (int i = 1; i < arr.length; i++) { // O(n) - один цикл
        if (arr[i] > max) {
            max = arr[i];
        }
    }
    return max;
}
```
- Временная сложность O(n) – цикл проходит по n элементам.
- Пространственная сложность O(1) – используется фиксированное количество памяти (только max).

#### Временная `O(1)`, пространственная `O(1)`
```java
public class ConstantTimeExample {
    public static int getFirstElement(int[] arr) {
        return arr[0]; // всегда выполняется за O(1)
    }
}
```
- Временная сложность O(1) – операция занимает фиксированное время независимо от кол-ва элементов, поэтому 
сложность O(1) (константная).
- Пространственная сложность O(1) – метод не выделяет новую память, а просто обращается к уже существующему массиву.

# 2. Основные алгоритмы сортировки знать/поимать принцип работы.


| **Алгоритм**            | **Описание**                                                                                                                                                   |
|-------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Сортировка пузырьком (Bubble Sort)**  | Один из простейших методов сортировки. Заключается в постепенном смещении элементов с большим значением в конец массива. Элементы последовательно сравниваются попарно, и если порядок в паре нарушен – меняются местами. |
| **Сортировка выбором (Selection Sort)** | Алгоритм ищет наименьший элемент в текущем списке и производит обмен его значения со значением первой неотсортированной позиции. То же самое происходит со вторым элементом с наименьшим значением. Цикл повторяется до тех пор, пока все элементы не займут нужную последовательность. |
| **Сортировка вставками (Insertion Sort)** | Применяется для вставки элементов массива на «свое место». Т.е. элемент исходного массива вставляется в новый массив, постепенно сортируя его. |
| **Сортировка слиянием (Merge Sort)**     | Следует принципу «разделяй и властвуй», согласно которому массив данных разделяется на равные части, которые сортируются по-отдельности. После они сливаются, в результате получается отсортированный массив. |
| **Быстрая сортировка (Quick Sort)**     | Считается одним из самых быстрых алгоритмов сортировки. Как и сортировка слиянием, работает по принципу «разделяй и властвуй». Отличие состоит в том, что выбирается некоторый опорный элемент для разделения массива на части. |

### Алгоритмическая сложность алгоритмов сортировки

| **Алгоритм сортировки**              | **Время работы в худшем случае** | **Время работы в среднем случае** | **Время работы в лучшем случае** | **Пространственная сложность** |
|--------------------------------------|---------------------------------|----------------------------------|---------------------------------|--------------------------------|
| **Сортировка пузырьком (Bubble Sort)** | O(n²)                           | O(n²)                            | O(n)                            | O(1)                           |
| **Сортировка выбором (Selection Sort)**| O(n²)                           | O(n²)                            | O(n²)                           | O(1)                           |
| **Сортировка вставками (Insertion Sort)** | O(n²)                           | O(n²)                            | O(n)                            | O(1)                           |
| **Сортировка слиянием (Merge Sort)**   | O(n log n)                      | O(n log n)                       | O(n log n)                      | O(n)                           |
| **Быстрая сортировка (Quick Sort)**   | O(n²)                           | O(n log n)                       | O(n)                            | O(log n)                       |


# 3. Пузырьковая сортировка, сложность, что по памяти.

**Принцип работы:**  
Пузырьковая сортировка – это простой алгоритм сортировки, который повторно проходит по массиву и сравнивает соседние 
элементы. Если порядок элементов нарушен, они меняются местами. Алгоритм повторяет эти шаги до тех пор, пока весь массив
не окажется отсортирован.

---

### **Алгоритмическая сложность:**

| **Тип сложности**               | **Сложность**  |
|----------------------------------|----------------|
| **Время работы в худшем случае** | O(n²)          |
| **Время работы в среднем случае**| O(n²)          |
| **Время работы в лучшем случае** | O(n)           |
| **Пространственная сложность**   | O(1)           |

---

###  **Объяснение сложности:**

1. **Время работы в худшем и среднем случае (O(n²)):**
    - В худшем случае (когда массив отсортирован в обратном порядке), алгоритм должен пройти по всем элементам дважды: внешний цикл повторяется `n` раз, а внутренний цикл выполняет `n-1`, `n-2`, ..., 1 сравнений. Это дает сложность `O(n²)`.

2. **Время работы в лучшем случае (O(n)):**
    - В лучшем случае (когда массив уже отсортирован), алгоритм выполнит один проход и больше не потребуется производить перестановок. Это приводит к сложности `O(n)`.

3. **Пространственная сложность (O(1)):**
    - Пузырьковая сортировка не требует дополнительной памяти, кроме самой сортируемой последовательности. В ней используется только несколько вспомогательных переменных для обмена элементов, поэтому ее пространственная сложность — **O(1)** (константная).

---
### **Реализация:**

```java
public class BubbleSort {

    // Метод для сортировки массива пузырьковым методом
    public static void bubbleSort(int[] arr) {
        int n = arr.length;
        
        // Проход по всем элементам массива
        for (int i = 0; i < n - 1; i++) {
            // Проход от 0 до n-i-1
            // Последние i элементов уже на своих местах
            for (int j = 0; j < n - i - 1; j++) {
                // Если элемент найден, который больше следующего
                if (arr[j] > arr[j + 1]) {
                    // Меняем элементы местами
                    int temp = arr[j];
                    arr[j] = arr[j + 1];
                    arr[j + 1] = temp;
                }
            }
        }
    }
}
```

# 4. Сортировка выбором, сложность, что по памяти.

**Принцип работы:**  
Сортировка выбором (Selection Sort) – это алгоритм, который сначала проводит операции сравнения и находит наименьший элемент,
а только потом помещает его в начало неотсортированного массива. Может быть как устойчивой, так и неустойчивой.

**Алгоритм выполнения:**
1. Находим номер минимального значения в текущем списке.
2. Производим обмен этого значения со значением первой неотсортированной позиции (обмен не нужен, если минимальный элемент уже находится на данной позиции).
3. Продолжаем сортировку оставшегося списка, исключив из рассмотрения ещё один элемент.

---

### **Алгоритмическая сложность:**

| **Тип сложности**               | **Сложность**  |
|----------------------------------|----------------|
| **Время работы в худшем случае** | O(n²)          |
| **Время работы в среднем случае**| O(n²)          |
| **Время работы в лучшем случае** | O(n²)          |
| **Пространственная сложность**   | O(1)           |

---

###  **Объяснение сложности:**

1. **Время работы в худшем, среднем и лучшем случае (O(n²)):**
    - В любом случае алгоритм должен пройти по всем элементам массива дважды. Таким образом сложность сортировки выбором равна `O(n²)`, где `n` - количество элементов в массиве.
2. **Пространственная сложность (O(1)):**
    - O(1) — так как алгоритм использует постоянное количество дополнительной памяти (только несколько переменных для хранения временных значений).

---

### **Реализация:**

```java
// Метод для сортировки массива сортировкой выбором
public static void selectionSort(int[] arr) {
   int n = arr.length;
   // Проход по всем элементам массива
   for (int i = 0; i < n - 1; i++) {
      // Сохраняем индекс элемента массива как минимальный
      int minIndex = i;
      // Проход по всем элементам массива + 1
      for (int j = i + 1; j < n; j++) {
         // Сравниваем все элементы с минимальным
         if (arr[j] < arr[minIndex]) {
            // Если найдено новое минимальное значение сохраняем его индекс в minIndex
            minIndex = j;
         }
      }
      // Если minIndex изменился, меняем его значение с i
      if (minIndex != i) {
         int tmp = arr[i];
         arr[i] = arr[minIndex];
         arr[minIndex] = tmp;
      }
   }
}
```

**Почему такая реализация неустойчива?**

Данная сортировка неустойчива, т.к. одинаковые элементы (с точки зрения той характеристики, по которой мы сортируем элементы) могут изменить своё положение.

Рассмотрим следующий массив из элементов, каждый из которых имеет два поля. Сортировка идет по первому полю.
Массив до сортировки: { (2, a), (2, b), (1, a) }
Уже после первой итерации внешнего цикла будем иметь отсортированную последовательность: { (1, a), (2, b), (2, a) } Теперь заметим, что взаимное расположение элементов (2, a) и (2, b) изменилось.
Таким образом, рассматриваемая реализация является неустойчивой.


# 5. Сортировка вставкой, сложность, что по памяти.

**Принцип работы:**  
Сортировка вставкой (Insertion Sort) -  алгоритм сортировки, в котором элементы входной последовательности просматриваются по одному, и каждый новый поступивший элемент размещается в подходящее место среди ранее упорядоченных элементов.
Данная сортировка является "устойчивой". Это значит, что одинаковые элементы не изменят свой порядок. Одинаковые с точки зрения характеристики, по которой мы сортируем.

**Алгоритм выполнения:**
1. Берём каждый неотсортированный элемент n и сравниваем его со значениями в отсортированном подмассиве справа налево,
   пока не определим подходящую позицию для n (то есть, в тот момент, когда находим первое число, которое меньше, чем n).
2. Затем сдвигаем все отсортированные элементы, которые находятся справа от этого числа вправо, чтобы образовалось место
   для n, и вставляем его туда, тем самым расширяя отсортированную часть массива.


---

### **Алгоритмическая сложность:**

| **Тип сложности**               | **Сложность**  |
|----------------------------------|----------------|
| **Время работы в худшем случае** | O(n²)          |
| **Время работы в среднем случае**| O(n²)          |
| **Время работы в лучшем случае** | O(n)           |
| **Пространственная сложность**   | O(1)           |

---

###  **Объяснение сложности:**

1. **Время работы в худшем случае (O(n²)):**
    - Наихудший случай для сортировки вставками возникает, когда входной массив отсортирован в обратном порядке.
      В этом случае каждый элемент необходимо сравнить и, возможно, поменять местами с каждым предыдущим элементом, что приводит к квадратичной временной сложности.
      Таким образом, наихудшая временная сложность составляет `O(n²)`, где `n` — количество элементов в массиве.
2. **Время работы в среднем случае (O(n²)):**
    - Средняя временная сложность сортировки вставками также равна `O(n²)`.
      Эта сложность возникает из-за особенностей алгоритма, который предполагает попарное сравнение и обмен местами для сортировки элементов.
      Хотя точное количество сравнений и перестановок может варьироваться в зависимости от входных данных, средняя временная сложность остаётся квадратичной.
3. **Время работы в лучшем случае (O(n)):**
    - Наилучшая временная сложность сортировки вставками достигается, когда входной массив уже отсортирован.
      В этом сценарии каждый элемент сравнивается с предыдущими элементами до тех пор, пока не потребуется замена, что приводит к линейной временной сложности.
      Таким образом, в лучшем случае временная сложность составляет `O(n)`, где n — количество элементов в массиве.
4. **Пространственная сложность (O(1)):**
    - Пространственная сложность сортировки вставками составляет `O(1)`, что означает, что она использует постоянное дополнительное пространство независимо от размера входных данных.

---
### **Реализация:**

```java
// Метод для сортировки массива сортировкой вставкой
public static void insertionSort(int[] arr) {
    int n = arr.length;
    for (int left = 0; left < n; left++) {
        // Вытаскиваем значение элемента
        int value = arr[left];
        // Перемещаемся по элементам, которые перед вытащенным элементом
        int i = left - 1;
        for (; i >= 0; i--) {
            // Если вытащили значение меньшее — передвигаем больший элемент дальше
            if (value < arr[i]) {
                arr[i + 1] = arr[i];
            } else {
                // Если вытащенный элемент больше — останавливаемся
                break;
            }
        }
        // В освободившееся место вставляем вытащенное значение
        arr[i + 1] = value;
    }
}
```

# 6. Быстрая сортировка, сложность, что по памяти.

**Принцип работы:**  
Быстрая сортировка (Quick Sort) - это алгоритм, изобретённый Тони Хоаром и использующийся для работы с большими массивами
данных. Необходим для автоматизации процессов, ускорения работы программ, анализа и вывода информации. Принцип работы
быстрой сортировки основан на принципе «разделяй и властвуй». Такие алгоритмы сначала делят крупный массив на два
подмассива поменьше, а затем рекурсивно упорядочивают элементы в подмножествах.

**Алгоритм выполнения:**
1. Выбор опорного элемента. Необходимо определить опорный элемент массива. Чаще всего им выступает самый левый или самый
   правый элемент массива.
2. Разделение элементов. Переупорядочивание массива и его компонентов осуществляется так, чтобы все составляющие меньше
   опорного располагались перед ним, а все элементы, которые больше «опоры» — после. Равные значения допускаются в любых направлениях.
3. Повторение. Здесь осуществляется рекурсия описанных ранее шагов для подмассива элементов с меньшими значениями,
   чем у опорного.
4. Базовый случа. Рекурсия останавливается, когда в подмассиве остаётся только один элемент, так как один элемент уже отсортирован.
---

### **Алгоритмическая сложность:**

| **Тип сложности**               | **Сложность** |
|----------------------------------|-------------|
| **Время работы в худшем случае** | O(n²)       |
| **Время работы в среднем случае**| O(n log n)  |
| **Время работы в лучшем случае** | O(n)        |
| **Пространственная сложность**   | O(log n)    |

---
###  **Объяснение сложности:**

1. **Время работы в худшем случае (O(n²)):**
    - В самом несбалансированном варианте каждое разделение даёт два подмассива размерами 1 и `n - 1`, то есть при каждом
      рекурсивном вызове больший массив будет на 1 короче, чем в предыдущий раз. Такое может произойти, если в качестве
      опорного на каждом этапе будет выбран элемент либо наименьший, либо наибольший из всех обрабатываемых. При простейшем
      выборе опорного элемента — первого или последнего в массиве — такой эффект даст уже отсортированный (в прямом или
      обратном порядке) массив, для среднего или любого другого фиксированного элемента «массив худшего случая» также может
      быть специально подобран. В этом случае потребуется `n - 1` операций разделения, а общее время работы составит `O(n²)`
      операций, то есть сортировка будет выполняться за квадратичное время. Но количество обменов и, соответственно, время
      работы — это не самый большой его недостаток. Хуже то, что в таком случае глубина рекурсии при выполнении алгоритма
      достигнет n, что будет означать n-кратное сохранение адреса возврата и локальных переменных процедуры разделения
      массивов. Для больших значений n худший случай может привести к исчерпанию памяти (переполнению стека) во время
      работы программы.
2. **Время работы в среднем случае (O(n log n)):**
    - Среднюю сложность при случайном распределении входных данных можно оценить лишь вероятностно.
      Прежде всего необходимо заметить, что в действительности необязательно, чтобы опорный элемент всякий раз делил
      массив на две одинаковых части. Например, если на каждом этапе будет происходить разделение на массивы длиной
      75 % и 25 % от исходного, глубина рекурсии будет равна ![log](https://wikimedia.org/api/rest_v1/media/math/render/svg/472aaeba13d1c610fa5606a60d0f46510934ffe6),
      а это по-прежнему даёт сложность `O(n log n)`. Вообще, при любом фиксированном соотношении между левой и правой частями
      разделения сложность алгоритма будет той же, только с разными константами. Будем считать «удачным» разделением такое,
      при котором опорный элемент окажется среди центральных 50 % элементов разделяемой части массива; ясно, что вероятность
      удачи при случайном распределении элементов составляет 0,5. При удачном разделении размеры выделенных подмассивов
      составят не менее 25 % и не более 75 % от исходного. Поскольку каждый выделенный подмассив так же будет иметь случайное
      распределение, все эти рассуждения применимы к любому этапу сортировки и любому исходному фрагменту массива. Удачное
      разделение даёт глубину рекурсии не более ![log](https://wikimedia.org/api/rest_v1/media/math/render/svg/472aaeba13d1c610fa5606a60d0f46510934ffe6).
      Поскольку вероятность удачи равна 0,5, для получения `k` удачных разделений в среднем потребуется `2 * k` рекурсивных
      вызовов, чтобы опорный элемент k раз оказался среди центральных 50 % массива. Применяя эти соображения, можно
      заключить, что в среднем глубина рекурсии не превысит ![log2](https://wikimedia.org/api/rest_v1/media/math/render/svg/db8cf70a69d9172523ae04ed2f080717584d1277),
      что равно `O(log n)`. А, поскольку на каждом уровне рекурсии по-прежнему выполняется не более `O(n)` операций, средняя
      сложность составит `O(n log n)`.
3. **Время работы в лучшем случае (O(n)):**
    - В наиболее сбалансированном варианте при каждой операции разделения массив делится на две одинаковые части,
      следовательно, максимальная глубина рекурсии, при которой размеры обрабатываемых подмассивов достигнут 1, составит
      log n. В результате количество сравнений, совершаемых быстрой сортировкой, было бы равно значению рекурсивного
      выражения ![выражение](https://wikimedia.org/api/rest_v1/media/math/render/svg/33a2ab6d4d004df9a9bc8ba1dbd7e19fb5d45f23)
      что даёт общую сложность алгоритма `O(n log n)`. При разделении на три частя общая сложность в лучшем случае `O(n)`.
4. **Пространственная сложность (O(log n)):**
    - `O(log n)` - она требуется для хранения стека вызовов рекурсии, если используется хвостовая рекурсия и опорные элементы выбираются удачно.

# 7. Сортировка слиянием, сложность, что по памяти.

**Принцип работы:**  
Сортировка слиянием (Merge Sort) - алгоритм сортировки, который упорядочивает списки (или другие структуры данных) в
определённом порядке. Эта сортировка — хороший пример использования принципа «разделяй и властвуй». Сначала задача
разбивается на несколько подзадач меньшего размера. Затем эти задачи решаются с помощью рекурсивного вызова или
непосредственно, если их размер достаточно мал. Наконец, их решения комбинируются, и получается решение исходной задачи.

**Алгоритм выполнения:**
1. Сортируемый массив разбивается на две части примерно одинакового размера.
2. Каждая из получившихся частей сортируется отдельно, например — тем же самым алгоритмом.
3. Два упорядоченных массива половинного размера соединяются в один.

---

### **Алгоритмическая сложность:**

| **Тип сложности**               | **Сложность** |
|----------------------------------|---------------|
| **Время работы в худшем случае** | O(n log n)    |
| **Время работы в среднем случае**| O(n log n)    |
| **Время работы в лучшем случае** | O(n log n)    |
| **Пространственная сложность**   | O(n)          |

---

###  **Объяснение сложности:**

1. **Время работы в худшем, среднем и лучшем случае (O(n log n)):**
    - Сортировка слиянием — один из немногих алгоритмов, которые гарантируют временную сложность `O(n log n)` в худшем,
      среднем и лучшем случаях. Все это — благодаря рекурсивному делению массива и упорядочиванию уже отсортированных
      подмассивов.
2. **Пространственная сложность (O(n)):**
    - `O(n)`, так как для реализации алгоритма требуется дополнительная память для хранения временных подмассивов,
      используемых на каждом этапе рекурсии.

---
### **Реализация:**

```java
// Метод для сортировки массива сортировкой слиянием с указанием границ участка сортировки
public static void mergeSort(int[] arr, int left, int right) {
   if (left < right) {
      // Разделим пополам входной массив
      int mid = (left + right) / 2;
      // Выполним рекурсивно данную функцию для двух половинок
      mergeSort(arr, left, mid);
      mergeSort(arr, mid + 1, right);
      // Метод сортировки левой и правой части массива
      merge(arr, left, mid, right);
   }
}
private static void merge(int[] arr, int left, int mid, int right) {
   int n1 = mid - left + 1;
   int n2 = right - mid;
   // Создаем массивы для левой и правой части
   int[] L = new int[n1];
   int[] R = new int[n2];
   // Копируем первую половину значений сортируемого массива в левый массив и вторую половину в правый
   System.arraycopy(arr, left, L, 0, n1);
   System.arraycopy(arr, mid + 1, R, 0, n2);
   int i = 0, j = 0;
   int k = left;
   // Сохраняем меньшие значения из левого и правого массива в сортируемый массив
   while (i < n1 && j < n2) {
      if (L[i] <= R[j]) {
         arr[k] = L[i];
         i++;
      } else {
         arr[k] = R[j];
         j++;
      }
      k++;
   }
   // Сохраняем оставшиеся значения из левого и правого массива в сортируемый массив
   while (i < n1) {
      arr[k] = L[i];
      i++;
      k++;
   }
   while (j < n2) {
      arr[k] = R[j];
      j++;
      k++;
   }
}
```



21. Есть два способа передачи параметров (значений):
    1 способ - Передача по значению (by value). Значения фактических параметров копируются.
    Вызываемый метод создает свою копию значений аргументов и затем ее использует.
    Поскольку работа ведется с копией, на исходный параметр это никак не влияет.
    2 способ - передача по ссылке (by reference). Параметры передаются как ссылка (адрес)
    на исходную переменную. Вызываемый метод не создает свою копию, а ссылается на исходное
    значение. Следовательно, изменения, сделанные в вызываемом методе, также будут отражены
    в исходном значении. В Java переменные хранятся следующим образом:локальные переменные,
    такие как примитивы и ссылки на объекты, создаются в стеке. Объекты — в куче (heap).
    Java всегда передает параметры по значению. Если требуется изменить значение к примеру объекта,то
    java делает копию объекта в heap и изменятся ссылка на этот объект. Если переменная примитивная то
    она изменяется в стеке. если переменная является объектом то создается новый объект в heap и
    на него меняется ссылка.
    Когда вы передаете примитивный тип (например, int, char, boolean), передается копия значения.
    Это значит, что любые изменения, сделанные в методе с этим параметром, не повлияют на оригинальную
    переменную, которая была передана в метод.
    Когда вы передаете объект (например, экземпляр класса или массив), передается копия ссылки на объект.
    Это означает, что метод получает доступ к оригинальному объекту, и изменения, внесенные в объект внутри метода,
    будут видны и вне метода. Однако если вы присвоите новую ссылку внутри метода, это не изменит оригинальную ссылку,
    переданную в метод.Таким образом, в Java можно сказать, что передача происходит по значению, но для объектов это значение
    является ссылкой на объект в памяти. Это создает некоторую путаницу, так как изменения в объекте будут отражены вне метода,
    но сама ссылка на объект остается неизменной.
# 27. Отличия вектора от ArrayList.
Вектор и ArrayList - это оба класса, которые реализуют интерфейс List в Java, но у них есть несколько ключевых отличий:
- Синхронизация: Вектор синхронизирован, что делает его потокобезопасным, но также замедляет производительность в однопоточных приложениях. ArrayList не синхронизирован и быстрее.
- Растяжимость: Вектор удваивает свой размер при достижении предела, тогда как ArrayList увеличивает размер на 50%.
- Итерация: Вектор предоставляет Enumeration для итерации, а ArrayList использует Iterator.

# 28. Что знаете об коллекциях типа List как доб. элемент\расширяется коллекция.
Коллекции типа List позволяют хранить упорядоченные элементы с возможностью дублирования. Основные операции включают добавление, удаление и доступ к элементам по индексу. При добавлении элементов коллекция автоматически расширяется, что происходит при достижении предела текущего массива. Например, размер массива увеличивается, и элементы копируются в новый массив.

# 29. Что знаете об коллекциях типа Set.
Коллекции типа Set представляют собой неупорядоченные коллекции, которые не допускают дублирования элементов. Основные реализации - HashSet (быстрый доступ, но без порядка), LinkedHashSet (сохраняет порядок добавления) и TreeSet (сортирует элементы). Используются, когда важна уникальность значений и не важен порядок.

# 30. Что знаете об коллекциях типа Queue.
Коллекции типа Queue представляют собой структуру данных "очередь", где элементы добавляются в один конец (очередь) и извлекаются с другого (голова очереди). Черты очереди:
- FIFO (first in, first out) – первый добавленный элемент будет первым, который будет извлечен.
- Реализации: LinkedList (реализует интерфейс Queue) и PriorityQueue (упорядочивает элементы на основе заданного порядка).
- Используются для решения задач, связанные с ожиданием процесса, как, например, в обработке задач на сервере.
[← Вернуться к вопросам](README.md)